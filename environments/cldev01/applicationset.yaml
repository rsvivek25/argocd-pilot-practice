---
# ApplicationSet for cldev01 environment
# Automatically generates Applications ONLY for charts that have corresponding values files
# in environments/cldev01/values/ directory

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cldev01-workloads
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  generators:
    - git:
        repoURL: https://github.com/rsvivek25/argocd-pilot-practice
        revision: main
        files:
          - path: 'environments/cldev01/values/*.yaml'
  template:
    metadata:
      name: '{{path[0]}}-cldev01'
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      sources:
        # Source 1: Helm chart with environment-specific values
        - repoURL: https://github.com/rsvivek25/argocd-pilot-practice
          targetRevision: main
          path: 'charts/{{path[0]}}'
          helm:
            releaseName: '{{path[0]}}'
            valueFiles:
              - ../../environments/cldev01/values/{{path[1]}}.yaml
        # Source 2: External ConfigMaps (pipeline-generated)
        - repoURL: https://github.com/rsvivek25/argocd-pilot-practice
          targetRevision: main
          path: 'environments/cldev01/config/{{path[0]}}'
          directory:
            recurse: false
      destination:
        server: https://kubernetes.default.svc
        namespace: cldev01
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/replicas
